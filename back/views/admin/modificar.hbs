<div class="container" style="margin:100px auto">
    <div class="row">
        <div class="col-6 offset-3">
            <h2>Modificar una novedad</h2>
            <form action="/admin/novedades/modificar" method="post">    
                <div class="mb-3 row">
                    <input type="hidden" value="{{novedad.id}}" name="id">
                </div>
                <div class="mb-3 row">
                    <textarea type="text" class="form-control" name="titulo">{{novedad.titulo}}</textarea>
                </div>
                <div class="mb-3 row">
                    <textarea type="text" class="form-control" name="subtitulo">{{novedad.subtitulo}}</textarea>
                </div>
                <div class="mb-3 row">
                    <textarea type="text" class="form-control" name="cuerpo">{{novedad.cuerpo}}</textarea>
                </div>
                <button type="submit" class="btn btn-primary">Modificar</button>
            </form>
            {{#if error}}
                <p>{{message}}</p>
            {{/if}}
        </div>
    </div>
</div>